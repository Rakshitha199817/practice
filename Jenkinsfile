pipeline {
    agent any

    stages {
        stage('mvn clean compile') {
            steps {
                echo 'Hello World'
            }
        }
        stage('mvn clean test'){
        steps{
            echo 'hi'
        }
        }
    }
}
